# Claude Code 配置（本地开发工具，不纳入版本控制）
.claude/
Claude.md
CLAUDE.md

# 构建输出
dist/
target/

# 操作系统文件
.DS_Store
Thumbs.db
Desktop.ini

# IDE / 编辑器
.idea/
.vscode/
*.swp
*.swo
*~

# 版本快照（ucx version auto 自动生成）
.ucx-snapshot.json

# 测试临时目录（黑盒回归测试的可再生 scratch：二进制、测试密钥、样本数据等）
# 正式测试报告已归档至 docs/test/problems/，此目录无需纳入版本控制
temp_test/

# 多语言 SDK：每个 sdk/<lang>/ 是独立子仓库（单独开源），父仓库不跟踪其内容，
# 仅保留索引(sdk/README.md)、wire-format 规范(sdk/UCX-FORMAT.md)与共享夹具(sdk/testdata/)。
/sdk/*/
!/sdk/testdata/
